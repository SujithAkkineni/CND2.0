extends layout

block content
  .form-container
    form(action='/sell', method='POST')
      h1.form-title Share Your Culinary Creation
      p(style='text-align: center; color: var(--text-light); margin-bottom: 30px;')
        | Help your neighbors experience authentic home-cooked meals by sharing your specialties

      .form-row
        label(for='name') Dish Name:
        input(type='text', id='name', name='name', placeholder='e.g., Masala Dosa, Chicken Biryani', required)

        label(for='seller') Chef's Name:
        input(type='text', id='seller', name='seller', placeholder='Your name or kitchen name', required)

      .form-row
        label(for='price') Price per Serving (₹):
        input(type='number', id='price', name='price', min='1', placeholder='e.g., 150', required)

        label(for='quantity') Available Servings:
        input(type='number', id='quantity', name='quantity', min='1', placeholder='e.g., 10', required)

      label(for='description') Dish Description:
      textarea(id='description', name='description', rows='4', placeholder='Describe your dish, ingredients, spice level, and what makes it special...')

      button(type='submit') 🎉 List My Dish
